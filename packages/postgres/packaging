# abort script on any command that exit with a non zero value
set -e

mkdir unpack_postgres
tar xzf postgres/postgres.tgz -C unpack_postgres

(
  cd unpack_postgres/postgres
  cp -af bin doc include jobs lib pg90-openscg.env share $BOSH_INSTALL_TARGET
)

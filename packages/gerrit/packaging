tar xf misc/tcl8-1.5.11rc1-src.tar
(
  cd tcl8.5.11/unix
  ./configure --prefix=${BOSH_INSTALL_TARGET}
  make
  make install
)


tar xf misc/expect5.45.tar
(
  cd expect5.45
  ./configure --prefix=${BOSH_INSTALL_TARGET}
  make
  make install
)

cp gerrit/gerrit-2.1-SNAPSHOT.war ${BOSH_INSTALL_TARGET}

---
name: gerrit
templates:
  gerrit_ctl: bin/gerrit_ctl
  gerrit_init.ctl.erb: bin/gerrit_init.ctl
  gerrit_expect.erb: bin/gerrit_expect
  ssh_expect.erb: bin/ssh_expect
  upgrade_expect.erb: bin/upgrade_expect
  gerrit_replication.erb: config/replication.config
  gerrit_config.erb: config/gerrit.config
  gerrit_secure.erb: config/secure.config
  gerrit_hooks.yml.erb: config/gerrit_hooks.yml
  nginx_ctl:      bin/nginx_ctl
  mime.types:     config/mime.types
  nginx.conf.erb: config/nginx.conf
packages:
  - git
  - ruby
  - nginx
  - java
  - gerrit
  - mysqlclient

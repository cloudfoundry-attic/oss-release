---
<% if properties.gerrit.pr_bounce %>
organization: <%= properties.gerrit.pr_bounce.organization %>
username: <%= properties.gerrit.pr_bounce.username %>
password: <%= properties.gerrit.pr_bounce.password %>
templates:
  closing: /var/vcap/jobs/gerrit/config/pr_bounce/closing.html
  closing_issue: /var/vcap/jobs/gerrit/config/pr_bounce/closing_issue.html
min_pr_num: {}
<% end %>
